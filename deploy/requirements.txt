# Gradio and spaces are supplied and pinned by the Hugging Face ZeroGPU
# runtime. Keep only the application-specific dependencies here.
openpyxl>=3.1
pandas>=2.0
numpy>=1.26
plotly>=5.20
psutil>=5.9
tomli>=2.0; python_version < "3.11"
